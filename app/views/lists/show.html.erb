<h1><%= @list.name %></h1>

<!-- <ul>
  <% @movies.each do |movie| %>
    <% bookmark = Bookmark.find_by(movie_id: movie.id, list_id: @list.id) %>
      <li><%= movie.title %> | <%= link_to "Delete", bookmark_path(bookmark), data: {turbo_method: :delete, turbo_confirm: "Are you sure ?"}, class: "delete_btn" %></li>
  <% end %>
</ul> -->

<ul>
  <% @list.bookmarks.each do |bookmark| %>
      <li><%= bookmark.movie.title %> | <%= link_to "Delete", bookmark_path(bookmark), data: {turbo_method: :delete, turbo_confirm: "Are you sure ?"}, class: "delete_btn" %></li>
  <% end %>
</ul>

<p><%= link_to "Add a movie", new_list_bookmark_path(@list) %> | <%= link_to "Back to my lists", lists_path %></p>
